# iOS 快捷指令制作教程（消息方案 · 最终版）

## 原理

```
手机                                    PC
────                                  ────
复制链接                               python main.py --collect
  ↓                                      ↓
快捷指令                             读取群聊消息
  ↓                                      ↓
发消息到飞书群聊                      解析链接 + 文案
  ↓                                      ↓
群里有消息了                          写入飞书表格 ✅
                                      ↓
                                    python main.py
                                      ↓
                                    AI 分析 + 回填
```

快捷指令只发一条文本消息到飞书群。不发 API、不拼 JSON、不鉴权。

---

## 第一步：飞书创建群聊 + 群机器人

### 1.1 创建群聊

```
飞书 → 左上角「+」→ 创建群聊
→ 群名称：笔记采集
→ 把你自己拉进去就行
```

### 1.2 添加群机器人获取 Webhook

```
群聊 → 右上角「...」→ 设置 → 群机器人
→ 添加机器人 → 自定义机器人
→ 名称：笔记采集助手
→ 安全设置：不用填（个人工具，不对外开放）
→ 点完成
→ 复制 Webhook URL
```

URL 格式：
```
https://open.feishu.cn/open-apis/bot/v2/hook/xxxxxxxxxx
```

> 记下来，快捷指令要用。

### 1.3 给应用开通消息权限

```
飞书开发者后台 → 应用「笔记采集」→ 权限管理
→ 搜索开通：im:message（消息与群聊）
→ 审批通过后，把应用也添加到「笔记采集」群聊里
  （群设置 → 添加机器人 → 找到「笔记采集」）
```

> 应用需要 im:message 权限才能读取群消息。群机器人不需要权限，只管发。

---

## 第二步：快捷指令制作

### 新建

```
快捷指令 App → 右上角「+」→ 名称「采集笔记」
```

### 按顺序添加操作

---

**① 获取剪贴板**

```
搜索：剪贴板 → 添加
```

---

**② 提取纯 URL**

```
搜索：获取 URL → 添加「从输入中获取 URL」

搜索：从列表 → 添加「从列表中获取项目」
→ 选「第一个项目」
```

---

**③ 存链接**

```
搜索：设定变量 → 添加
→ 变量名：url
→ 值：上一步的结果
```

---

**④ 清洗内容（去 URL 和尾部废话）**

```
搜索：拆分文本 → 添加
→ 文本：选「获取剪贴板」的结果
→ 分隔符：自定义 → 输入： http
  （空格 + http）

搜索：从列表中获取项目 → 添加
→ 选「第一个项目」
```

---

**⑤ 替换特殊字符**

```
搜索：替换文本 → 添加
→ 文本：上一步的结果
→ 查找："
→ 替换为：'

搜索：替换文本 → 再添加
→ 文本：上一步的结果
→ 查找：按 Return / 换行键
→ 替换为：空格
```

---

**⑥ 拼消息文本**

```
搜索：文本 → 添加
→ 输入框中拼：
  笔记链接
→ 换行
→ 选变量「url」
→ 换行
→ 输入：笔记内容
→ 换行
→ 选「上一步替换后的文本」
```

> 最终消息格式：
> ```
> 笔记链接
> https://xhslink.com/o/xxx
> 笔记内容
> 瑞幸新品奶霜青露茉莉好不好喝...
> ```

---

**⑦ 发到飞书群**

```
搜索：获取 URL 内容 → 添加
→ URL：你的 Webhook 地址
  https://open.feishu.cn/open-apis/bot/v2/hook/xxxxxxxxxx
→ 「>」展开：
  → 方法：POST
  → 头部添加 1 个：
      Content-Type   值：application/json
  → 请求体：JSON
  → 内容填（手打，不用变量）：
```

```json
{"msg_type":"text","content":{"text":"替换为⑥的文本"}}
```

> ⚠️ 捷径里手打这个 JSON 模板时，`替换为⑥的文本` 那部分 → 删掉引号中间的占位文字 → 点输入框 → 选变量「文本」（操作⑥的结果）。

---

**⑧ 通知**

```
搜索：显示通知 → 添加
→ 标题：已发送
→ 正文：笔记已发到飞书群
```

---

## 第三步：添加到桌面

```
快捷指令首页 → 长按「采集笔记」→ 分享 → 添加到主屏幕
```

---

## 第四步：日常使用

### 手机

```
小红书 → 复制链接 → 点桌面图标 → 完成
```

群里自动出现一条消息：
```
笔记链接
https://xhslink.com/o/xxx
笔记内容
瑞幸新品奶霜青露茉莉好不好喝...
```

### 电脑

```bash
python main.py
```

一步到位：自动采集群消息 → 抓全文 → AI 分析 → 回填表格。

---

## 第五步：每天工作流

```bash
cd D:\Project\xhs-feishu
python main.py      # 采集 + AI 分析，一步到位
```

---

## 这个方案的优势

| 对比 | API 方案 | 消息方案 |
|------|:---:|:---:|
| 快捷指令 | 14 步含鉴权 | **7 步** |
| JSON 嵌套 | 3 层 | **0 层（纯文本消息）** |
| 飞书鉴权 | 需要 | **不需要（Webhook 自带鉴权）** |
| iOS 序列化坑 | 全踩过 | **不存在** |
| 网络中断 | 经常 | **不会** |
| 格式 | `{"fields":{...}}` | 一段纯文本 |

---

## 参数速查

| 参数 | 值 |
|------|-----|
| 群聊名称 | 笔记采集 |
| 群机器人 Webhook | 从群设置里获取 |
| App Token | 你的多维表格 app_token |
| Table ID | 你的 table_id |
